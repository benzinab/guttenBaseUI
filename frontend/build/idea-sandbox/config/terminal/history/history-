ls
mkdir repo
cd repo/
git clone https://bitbucket.encoway-services.de/scm/khs/khs.git
sudo apt install git
git clone https://bitbucket.encoway-services.de/scm/khs/khs.git 
ls
git clone https://bitbucket.encoway-services.de/scm/khs/khs.git 
sudo nano /etc/bluetooth/main.conf
sudo /etc/init.d/bluetooth restart
javac -version 
hahahahah
javac -target 11 
cd src/main/java/de/akquinet/jboscc/guttenBasePlugin/
javac -target 11 ShowAction.java 
sudo /etc/init.d/bluetooth restart
sudo nano /etc/bluetooth/main.conf
sudo /etc/init.d/bluetooth restart
gnome-system-monitor
apt-get update && apt-get upgrade
sudo apt-get update && apt-get upgrade
sudo chmod +xrw /var/lib/dpkg/lock-frontend
sudo apt-get update && apt-get upgrade
sudo apt-get upgrade
java --help
java --version
javac -version 
sudo apt install default-jdk
JAVA_HOME
cat JAVA_HOME
javac -version 
java -version
$JAVA_HOME
cat $JAVA_HOME
echo $JAVA_HOME
vi ~/.bashrc OR vi ~/.bash_profile
java -version
javac -version 
sudo /etc/init.d/bluetooth restart
sudo nano /etc/bluetooth/main.conf
sudo /etc/init.d/bluetooth restart
~/.local/share/JetBrains/Toolbox/apps
~/.local/share/JetBrains/Toolbox/
~/.local/share/
cd ~/.local/share/JetBrains/IntelliJIdea2020.2/
ls
chmod +xrw nap/intellij-idea-ultimate/current
chmod +xrw snap/intellij-idea-ultimate/current
sudo chmod +xrw snap/intellij-idea-ultimate/current
cd /snap/intellij-idea-ultimate/current
ls
sudo chmod +rwx plugins/
ls
gradle clean install 
[A
gradle clean install 
./gradlew clean build
gradle clean build
